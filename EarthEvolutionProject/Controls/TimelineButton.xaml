<UserControl x:Class="EarthEvolutionProject.Controls.TimelineButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             Height="55">

    <Grid ClipToBounds="False">
        <Border x:Name="ActiveFrame"
                Margin="0,0,0,0"
                CornerRadius="2"
                BorderBrush="#A7BE4B"
                BorderThickness="2"
                Background="Transparent"
                Visibility="Collapsed"/>

        <Button x:Name="ActionButton"
                Click="ActionButton_Click"
                Cursor="Hand"
                BorderBrush="Transparent"
                BorderThickness="3"
                Margin="0,0"
                Padding="0"
                VerticalAlignment="Stretch"
                VerticalContentAlignment="Center"
                SnapsToDevicePixels="True"
                Background="#2B7793">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="1.5">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>

            <Grid ClipToBounds="False">
                <Path x:Name="ActiveArrow"
                      Data="M 0,0 L 18,0 L 9,11 Z"
                      Fill="#E6E6D8"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Top"
                      Margin="0,-10,0,0"
                      Visibility="Collapsed"/>

                <StackPanel VerticalAlignment="Center" Margin="0,6">
                    <TextBlock x:Name="TitleText"
                               Foreground="White"
                               FontSize="13"
                               FontWeight="SemiBold"
                               HorizontalAlignment="Center"
                               TextAlignment="Center"/>
                    <TextBlock x:Name="YearsText"
                               Foreground="#E0E0E0"
                               FontSize="10"
                               HorizontalAlignment="Center"
                               TextAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Button>
    </Grid>
</UserControl>