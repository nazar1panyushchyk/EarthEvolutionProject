<Window x:Class="EarthEvolutionProject.Views.FactWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Чи знаєте ви, що..." 
        WindowState="Maximized"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        KeyDown="Window_KeyDown"
        Opacity="0">
    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.2"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Grid Background="#01000000" MouseDown="OutsideGrid_MouseDown">
        <Border Background="#1E1E24" 
                CornerRadius="15" 
                BorderBrush="#3A3A42" 
                BorderThickness="1.5"
                VerticalAlignment="Center" 
                HorizontalAlignment="Center"
                Width="430" Height="230"
                MouseDown="Border_MouseDown">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <TextBlock Text="💡" FontSize="22" Margin="0,0,10,0"/>
                        <TextBlock Text="Цікавий факт дня" FontSize="18" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                    </StackPanel>

                    <Button Content="✕" 
                            HorizontalAlignment="Right" 
                            VerticalAlignment="Center"
                            Foreground="#8E8E93" 
                            Background="Transparent" 
                            BorderThickness="0" 
                            FontSize="16" 
                            Cursor="Hand"
                            Click="CloseButton_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>

                <TextBlock x:Name="FactTextBlock" 
                           Grid.Row="1" 
                           Text="Тут буде текст випадкового еволюційного факту..." 
                           Foreground="#D1D1D6" 
                           FontSize="14" 
                           TextWrapping="Wrap" 
                           VerticalAlignment="Center"
                           LineHeight="20"/>

                <Grid Grid.Row="2" Margin="0,15,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <CheckBox x:Name="DontShowAgainCheckBox" 
                              Grid.Column="0" 
                              Content="Більше не показувати" 
                              Foreground="#8E8E93" 
                              VerticalAlignment="Center"
                              Cursor="Hand"/>

                    <Button Grid.Column="1" 
                            Content="Зрозуміло" 
                            Width="100" Height="32" 
                            Background="#4A90E2" Foreground="White" 
                            FontWeight="Bold" BorderThickness="0" 
                            Cursor="Hand"
                            Click="CloseButton_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>