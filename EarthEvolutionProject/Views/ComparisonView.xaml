<UserControl x:Class="EarthEvolutionProject.Views.ComparisonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource SecondaryBackgroundBrush}">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="ПОРІВНЯЛЬНА ШКАЛА ІСНУВАННЯ"
                   Foreground="{DynamicResource PrimaryTextBrush}" FontSize="28" FontWeight="Bold" Margin="0,0,0,25"/>

        <Grid Grid.Row="1" Margin="0,0,0,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Істота 1:" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="16" Margin="0,0,0,8"/>
                <ComboBox x:Name="ComboOrg1" Height="45" FontSize="16" DisplayMemberPath="CommonName"
                          SelectionChanged="OnOrganismSelectionChanged"/>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Істота 2:" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="16" Margin="0,0,0,8"/>
                <ComboBox x:Name="ComboOrg2" Height="45" FontSize="16" DisplayMemberPath="CommonName"
                          SelectionChanged="OnOrganismSelectionChanged"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" Background="{DynamicResource PrimaryBackgroundBrush}"
                CornerRadius="12" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="2">
            <Grid Margin="15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Canvas x:Name="AxisCanvas" Grid.Column="0"/>

                <Grid Grid.Column="1">
                    <Canvas x:Name="Org1Canvas"/>
                </Grid>

                <Grid Grid.Column="2">
                    <Canvas x:Name="Org2Canvas"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>