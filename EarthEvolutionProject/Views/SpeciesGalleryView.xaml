<UserControl x:Class="EarthEvolutionProject.Views.SpeciesGalleryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:EarthEvolutionProject.Views"
             xmlns:controls="clr-namespace:EarthEvolutionProject.Controls"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Border Background="{DynamicResource SecondaryBackgroundBrush}" CornerRadius="15" Padding="12">
        <Grid>

            <Grid x:Name="GalleryListState" Visibility="Visible">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <TextBlock Text="ГАЛЕРЕЯ ВИДІВ" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="18" FontWeight="Light" VerticalAlignment="Center"/>
                    <Button Content="➕ Додати організм" 
                        HorizontalAlignment="Right" 
                        Padding="10,5"
                        Click="AddOrganismButton_Click" 
                        Style="{DynamicResource PrimaryButtonStyle}"/>      
                </Grid>

                <ScrollViewer Grid.Row="1" x:Name="GalleryScrollViewer" 
                    VerticalScrollBarVisibility="Visible" Padding="0,0,5,0">
                    <ItemsControl Grid.Row="1" x:Name="SpeciesItemsControl" VerticalAlignment="Top">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="3"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <controls:OrganismCardView Cursor="Hand" PreviewMouseDown="OrganismCard_MouseDown"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

            </Grid>

            <Grid x:Name="SpeciesDetailState" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Button Content="← Назад до галереї" Click="BackToList_Click"
                            Background="Transparent" Foreground="{DynamicResource AccentBlueBrush}" BorderThickness="0" 
                            HorizontalAlignment="Left" FontSize="12" Cursor="Hand"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="✏️ Редагувати" Click="EditOrganismButton_Click" Margin="0,0,10,0" Padding="8,3" Style="{DynamicResource PrimaryButtonStyle}"/>
                        <Button Content="🗑️ Видалити" Click="DeleteOrganismButton_Click" Background="#FF4B4B" Foreground="White" Padding="8,3"/>
                    </StackPanel>
                </Grid>

                <Border Grid.Row="1" 
                        Background="{DynamicResource PrimaryBackgroundBrush}" 
                        CornerRadius="12" 
                        Height="200" 
                        Margin="0,0,0,20" 
                        ClipToBounds="True">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" Direction="270" Opacity="0.3" ShadowDepth="2"/>
                    </Border.Effect>
                    <Image x:Name="DetailImage" 
                           Stretch="Uniform" 
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           RenderOptions.BitmapScalingMode="HighQuality"/>
                </Border>

                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock x:Name="DetailCommonName" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="20" FontWeight="Bold" TextWrapping="Wrap" Margin="0,0,10,0"/>
                        <TextBlock x:Name="DetailScientificName" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="13" FontStyle="Italic" Margin="0,2,10,15"/>

                        <Grid Margin="0,0,10,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="ТИП" Foreground="{DynamicResource AccentBlueBrush}" FontSize="9" FontWeight="Bold"/>
                                <TextBlock x:Name="DetailType" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="12" Margin="0,2,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="ПЕРІОД" Foreground="{DynamicResource AccentBlueBrush}" FontSize="9" FontWeight="Bold"/>
                                <TextBlock x:Name="DetailExistence" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="12" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="СПОСІБ ЖИТТЯ" Foreground="{DynamicResource AccentBlueBrush}" FontSize="9" FontWeight="Bold" Margin="0,0,10,5"/>
                        <TextBlock x:Name="DetailLifestyle" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="13" TextWrapping="Wrap" LineHeight="18" Margin="0,0,10,0"/>
                    </StackPanel>
                </ScrollViewer>
            </Grid>

            <Grid x:Name="CrudFormGrid" Visibility="Collapsed" Background="{DynamicResource SecondaryBackgroundBrush}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="15">
                        <TextBlock x:Name="FormTitleTextBlock" Text="Додавання нового організму" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,15"/>

                        <TextBlock Text="Назва (українською):" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,5,0,2"/>
                        <TextBox x:Name="CommonNameTextBox" Height="30" VerticalContentAlignment="Center"/>

                        <TextBlock Text="Наукова назва (латинь):" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,10,0,2"/>
                        <TextBox x:Name="ScientificNameTextBox" Height="30" VerticalContentAlignment="Center"/>

                        <TextBlock Text="Тип організму:" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,10,0,2"/>
                        <ComboBox x:Name="TypeComboBox" Height="30" VerticalContentAlignment="Center"/>

                        <TextBlock Text="Геологічний період:" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,10,0,2"/>
                        <ComboBox x:Name="PeriodComboBox" Height="30" VerticalContentAlignment="Center" DisplayMemberPath="Name" SelectedValuePath="Id"/>

                        <StackPanel Margin="0,10,0,10">
                            <DockPanel LastChildFill="False" Margin="0,0,0,5">
                                <TextBlock Text="Час існування (років тому):" 
                                    Foreground="{DynamicResource PrimaryTextBrush}" 
                                    FontWeight="SemiBold" FontSize="13" DockPanel.Dock="Left"/>

                                <CheckBox x:Name="IsRangeCheckBox" 
                                    Content="Діапазон (проміжок часу)" 
                                    Foreground="{DynamicResource SecondaryTextBrush}" 
                                    FontSize="12" 
                                    DockPanel.Dock="Right" 
                                    VerticalAlignment="Center"
                                    Cursor="Hand"
                                    Checked="IsRangeCheckBox_Changed" 
                                    Unchecked="IsRangeCheckBox_Changed"/>
                            </DockPanel>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition x:Name="SeparatorColumn" Width="Auto"/>
                                    <ColumnDefinition x:Name="ToValueColumn" Width="0"/>
                                    <ColumnDefinition x:Name="ToUnitColumn" Width="0"/>
                                </Grid.ColumnDefinitions>

                                <TextBox x:Name="FromValueTextBox" 
                                     Grid.Column="0" Height="32" 
                                     VerticalContentAlignment="Center" Padding="8,0"
                                     Background="{DynamicResource SecondaryBackgroundBrush}" 
                                     Foreground="{DynamicResource PrimaryTextBrush}"   
                                     BorderBrush="{DynamicResource BorderBrush}"
                                     PreviewTextInput="NumberValidationTextBox"/>

                                <ComboBox x:Name="FromUnitComboBox" 
                                        Grid.Column="1" Width="80" Height="32" Margin="5,0,0,0"
                                        VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
                                        Background="{DynamicResource SecondaryBackgroundBrush}" 
                                        Foreground="{DynamicResource PrimaryTextBrush}"
                                        SelectedIndex="0">
                                    <ComboBox.Style>
                                        <Style TargetType="ComboBox">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ComboBox">
                                                        <Grid>
                                                            <Border x:Name="ToggleButtonBackground" 
                                                                Background="{TemplateBinding Background}" 
                                                                BorderBrush="{DynamicResource BorderBrush}" 
                                                                BorderThickness="1" 
                                                                CornerRadius="4"/>

                                                            <ToggleButton x:Name="PART_ToggleButton" 
                                                                Background="Transparent" 
                                                                BorderThickness="0"
                                                                HorizontalContentAlignment="Stretch" 
                                                                VerticalContentAlignment="Stretch"
                                                                IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                                Focusable="False"/>

                                                            <ContentPresenter x:Name="ContentSite" 
                                                                IsHitTestVisible="False" 
                                                                Content="{TemplateBinding SelectionBoxItem}" 
                                                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" 
                                                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}" 
                                                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                                                Margin="10,0,22,0"/>

                                                            <Path x:Name="Arrow" 
                                                                IsHitTestVisible="False"
                                                                Fill="{TemplateBinding Foreground}" 
                                                                Data="M 0 0 L 3.5 4 L 7 0 Z" 
                                                                HorizontalAlignment="Right" 
                                                                VerticalAlignment="Center" 
                                                                Margin="0,0,8,0"/>

                                                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                                                <Grid x:Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                                                    <Border x:Name="DropDownBorder" Background="{DynamicResource SecondaryBackgroundBrush}" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}" CornerRadius="4"/>
                                                                    <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                                                    </ScrollViewer>
                                                                </Grid>
                                                            </Popup>
                                                        </Grid>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ComboBox.Style>
                                    <ComboBox.ItemContainerStyle>
                                        <Style TargetType="ComboBoxItem">
                                            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                                            <Setter Property="Background" Value="{DynamicResource SecondaryBackgroundBrush}"/>
                                        </Style>
                                    </ComboBox.ItemContainerStyle>
                                    <ComboBoxItem Content="млн"/>
                                    <ComboBoxItem Content="тис"/>
                                    <ComboBoxItem Content="р."/>
                                </ComboBox>

                                <TextBlock x:Name="RangeSeparatorText" 
                                    Grid.Column="2" Text=" — " 
                                    Foreground="{DynamicResource PrimaryTextBrush}" 
                                    VerticalAlignment="Center" HorizontalAlignment="Center" 
                                    Margin="5,0,5,0" Visibility="Collapsed"/>

                                <TextBox x:Name="ToValueTextBox" 
                                     Grid.Column="3" Height="32" Margin="5,0,0,0"
                                     VerticalContentAlignment="Center" Padding="8,0"
                                     Background="{DynamicResource SecondaryBackgroundBrush}" 
                                     Foreground="{DynamicResource PrimaryTextBrush}" 
                                     BorderBrush="{DynamicResource BorderBrush}"
                                     Visibility="Collapsed"
                                     PreviewTextInput="NumberValidationTextBox"/>

                                <ComboBox x:Name="ToUnitComboBox" 
                                        Grid.Column="4" Width="80" Height="32" Margin="5,0,0,0"
                                        VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
                                        Background="{DynamicResource SecondaryBackgroundBrush}" 
                                        Foreground="{DynamicResource PrimaryTextBrush}"
                                        SelectedIndex="0"
                                        Visibility="Collapsed">
                                    <ComboBox.Style>
                                        <Style TargetType="ComboBox">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ComboBox">
                                                        <Grid>
                                                            <Border x:Name="ToggleButtonBackground" Background="{TemplateBinding Background}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4"/>
                                                            <ToggleButton x:Name="PART_ToggleButton" 
                                                                Background="Transparent" 
                                                                BorderThickness="0"
                                                                HorizontalContentAlignment="Stretch" 
                                                                VerticalContentAlignment="Stretch"
                                                                IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                                Focusable="False"/>
                                                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="10,0,22,0"/>
                                                            <Path x:Name="Arrow" Fill="{TemplateBinding Foreground}" Data="M 0 0 L 3.5 4 L 7 0 Z" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                                                <Grid x:Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                                                    <Border x:Name="DropDownBorder" Background="{DynamicResource SecondaryBackgroundBrush}" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}" CornerRadius="4"/>
                                                                    <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                                                    </ScrollViewer>
                                                                </Grid>
                                                            </Popup>
                                                        </Grid>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ComboBox.Style>
                                    <ComboBox.ItemContainerStyle>
                                        <Style TargetType="ComboBoxItem">
                                            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                                            <Setter Property="Background" Value="{DynamicResource SecondaryBackgroundBrush}"/>
                                        </Style>
                                    </ComboBox.ItemContainerStyle>
                                    <ComboBoxItem Content="млн"/>
                                    <ComboBoxItem Content="тис"/>
                                    <ComboBoxItem Content="р."/>
                                </ComboBox>
                            </Grid>
                        </StackPanel>

                        <TextBlock Text="Спосіб життя та опис:" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,10,0,2"/>
                        <TextBox x:Name="LifestyleTextBox" Height="80" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

                        <TextBlock Text="Зображення організму:" Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,10,0,2"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="ImagePathTextBox" IsReadOnly="True" Height="30" VerticalContentAlignment="Center" Margin="0,0,10,0"/>
                            <Button Grid.Column="1" Content="📁 Огляд..." Width="90" Click="BrowseImageButton_Click" Style="{DynamicResource PrimaryButtonStyle}"/>
                        </Grid>

                        <Grid Margin="0,20,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Content="Скасувати" Height="35" Margin="0,0,10,0" Click="CancelFormButton_Click" Style="{DynamicResource PrimaryButtonStyle}"/>
                            <Button Grid.Column="1" Content="Зберегти" Height="35" Background="{DynamicResource PrimaryBlueBrush}" Foreground="White" FontWeight="Bold" Click="SaveFormButton_Click" Style="{DynamicResource PrimaryButtonStyle}"/>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Grid>

        </Grid>
    </Border>
</UserControl>